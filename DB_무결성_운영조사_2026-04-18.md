# SAMC DB 무결성·운영 관점 전수조사 (2026-04-18)

> **성격**: 운영·무결성·기능↔DB 매핑 분석 (정적 스키마 카탈로그는 [schema_overview.md](schema_overview.md) 참고)
> **기준 커밋**: `c013ed0` (branch: BC)
> **Supabase 프로젝트**: `bnfgbwwibnljynwgkgpt`
> **조사 범위**: `backend/db/migrations/001~013`, `backend/db/seed/01~07`, `backend/db/combined_schema.sql`, `backend/db/feature4/schema.sql`, `backend/services/**`, `backend/routers/**`

---

## 0. 본 문서와 schema_overview.md 관계

| 문서 | 담당 영역 |
|---|---|
| [schema_overview.md](schema_overview.md) | **정적 스키마 카탈로그** — Mermaid ERD, 컬럼 정의, 인덱스/RLS 요약, Prefix 규칙 |
| **본 문서** | **운영·무결성·코드 연동 분석** — 시드 실적, 기능별 READ/WRITE 매핑, 리스크 목록, migration 011~013 배경, 코드↔DB 괴리 |

테이블 목록·컬럼 세부는 `schema_overview.md` §2 ERD를 참조하세요.

---

## 1. 기능별 DB 사용 매핑 (코드 분석 기반)

### F1 — 수입 가능 여부 판정 (담당: 병찬)

| 구분 | 테이블 | 사용처 | 쿼리 패턴 |
|---|---|---|---|
| **READ** | `f1_forbidden_ingredients` | `services/step1_ingredients_check.py` (Step 0 게이트) | 전체 조회 후 Python에서 정규화·양방향 substring 검사 (15행) |
| READ | `f1_allowed_ingredients` | `services/step1_ingredients_check.py` | 5단계 매칭 체인 (정확명→INS→CAS→학명 ILIKE→`search_f1_ingredients_trgm` RPC) |
| READ | `f1_additive_limits` | `services/step3_standards.py` | `(food_type, additive_name)` 조회 + `combined_group` 합산 검증 + `conversion_factor` 환산 |
| READ | `f1_safety_standards` | `services/step3_standards.py` | `(food_type, standard_type, target_name)` UNIQUE 조회 |
| READ | `f1_ingredient_synonyms` | **미사용** | ⚠ 테이블만 존재, 시드 0건, 코드 미연동 |
| READ | `f1_law_chunks` | `routers/db_manager.py` (admin) | Pinecone 재임베딩·삭제 메타 관리 |
| WRITE | `f1_escalation_logs` | `services/step1`·`step3` | ⚠ 현재 INSERT 미구현 — dict만 반환 후 `pipeline_steps.ai_result` JSONB에 직렬화 저장 |
| WRITE | `pipeline_steps` | `routers/feature1.py` | `step_key='1'`, `ai_result`·`final_result`·`law_references` |

### F2 — 식품유형 분류 (담당: 아람)

| 구분 | 테이블/리소스 | 사용처 | 비고 |
|---|---|---|---|
| READ | `f2_required_documents` | `services/feature2*.py` | `food_type` 기반 필수서류 |
| READ | **`f2_food_type_classification`** | `routers/feature2.py:88` | ⚠ **스키마 미정의** — `combined_schema.sql` 에 없음 |
| READ | Pinecone `samc-a` 인덱스 | `services/feature2*.py` | OpenAI `text-embedding-3-small`, top-5 청크 |
| WRITE | `pipeline_steps` | `routers/feature2.py` | `step_key='2'` |

### F3 — 수입 필요서류 안내

**테이블 전무**. `f3_` prefix 예정이나 담당자·컬럼 미정.

### F4 — 수출국표시사항 검토 (담당: 성은)

| 구분 | 테이블 | 사용처 | 비고 |
|---|---|---|---|
| READ | `f4_law_documents` | 법령 메타 조회 | 시드 0건 |
| READ | `f4_prohibited_expressions` | 키워드 1차 필터 | 시드 0건 (LLM 동적 추출 의존) |
| READ | `f4_image_violation_types` | 이미지 위반 유형 목록 | 시드 0건 (`source='seed'\|'auto'`) |
| READ | Pinecone `samc-law-f4` | 법령 청크 RAG | |
| WRITE | `pipeline_steps` | `step_key='B'` | |

### F5 — 한글표시사항 검토 (담당: 세연)

| 구분 | 테이블 | 사용처 | 시드 |
|---|---|---|---|
| READ | `f5_label_rules`, `f5_allergy_list`, `f5_additive_label_rules`, `f5_gmo_ingredients` | 표시 규칙 조회 | 0건 |
| READ | `f5_thresholds`, `f5_ingredient_list`, `f5_required_documents` | 기준·원료·서류 | 0건 |
| READ | `f5_law_chunks` | RAG 청크 | ⚠ 컬럼 정의 미완료 |
| WRITE | `pipeline_steps` | `step_key='6'` | |

---

## 2. 시드 데이터 인벤토리 (실측)

### F1 시드 합계: **229건**

| 시드 파일 | 테이블 | 건수 | 비고 |
|---|---|---:|---|
| `01_f1_ingredients_permitted.sql` | `f1_allowed_ingredients` | 85 | `allowed_status='permitted'` (별표1) |
| `02_f1_ingredients_restricted.sql` | `f1_allowed_ingredients` | 10 | `restricted` (별표2, `conditions` 필수) |
| `03_f1_ingredients_prohibited.sql` | `f1_allowed_ingredients` | 15 | `prohibited` (별표3) |
| `04_f1_forbidden_ingredients.sql` | `f1_forbidden_ingredients` | 15 | drug 3 · endangered 3 · unauthorized 6 · toxin 3 |
| `05_f1_thresholds_core.sql` | `f1_additive_limits` / `f1_safety_standards` | 13 / 10 | core |
| `06_f1_additive_limits_expanded.sql` | `f1_additive_limits` | 51 | expanded |
| `07_f1_safety_standards_expanded.sql` | `f1_safety_standards` | 30 | expanded |
| **합계** | **3개 테이블** | **229** | — |

### 테이블별 총합

| 테이블 | 시드 건수 |
|---|---:|
| `f1_allowed_ingredients` | 110 (85 + 10 + 15) |
| `f1_forbidden_ingredients` | 15 |
| `f1_additive_limits` | 64 (13 + 51) |
| `f1_safety_standards` | 40 (10 + 30) |
| `f1_ingredient_synonyms` | 0 |
| `f1_escalation_logs`·`f1_law_chunks` | 0 (런타임 누적) |
| **F2/F4/F5 전 테이블** | **0** |

### F2/F4/F5 시드 공백

- **F2**: `f2_required_documents` 시드 없음. 데이터 원천이 Pinecone이라 DB 중심성 약함.
- **F4**: 세 테이블 모두 시드 0. `f4_prohibited_expressions`는 LLM 자동 추출에 의존.
- **F5**: 8개 테이블 모두 시드 0. 컬럼 정의만 존재.

---

## 3. 테이블 완성도 분류 (총 34개)

| 상태 | 개수 | 내용 |
|---|---:|---|
| ✅ **완성** (컬럼+시드) | 4 | `f1_allowed_ingredients`, `f1_forbidden_ingredients`, `f1_additive_limits`, `f1_safety_standards` |
| ✅ **완성** (컬럼, 시드 런타임) | 8 | 공통 5 (`cases`·`documents`·`pipeline_steps`·`law_alerts`·`feedback_logs`) + `f1_escalation_logs`·`f1_law_chunks`·`f1_ingredient_synonyms` |
| 🟡 **컬럼 정의** (시드 0) | 11 | F2 1개 + F4 3개 + F5 확정 7개 |
| ⚪ **스켈레톤** (`id + created_at`만) | 11 | F1 10개(`f1_food_types`·`f1_regulations`·`f1_reviews`·`f1_allergens`·`f1_analytics_events`·`f1_flavor_codes`·`f1_material_codes`·`f1_process_codes`·`f1_regulation_updates`·`f1_review_items`) + F5 1개(`f5_law_chunks`) |
| **합계** | **34** | schema_overview.md §4 일치 |

> 초기 보고에서 "40개"로 집계한 부분은 F1 스켈레톤을 중복 계산한 오류였음. **정본은 34개.**

---

## 4. 무결성 평가

### 강점

| # | 항목 | 근거 |
|---|---|---|
| 1 | 다층 제약 | PK(UUID) + UNIQUE partial(ins/cas) + CHECK(enum·수치) + FK(auth.users, cases) |
| 2 | GIN trgm 인덱스 | `f1_allowed_ingredients.name_ko/_en/_scientific` 3종, `f1_forbidden_ingredients.aliases` (배열 포함검사) |
| 3 | `is_verified` + `law_source` 컨벤션 | §8 룰 준수, Step 1/3 판정은 `is_verified=true`만 사용 |
| 4 | Backfill 멱등성 | migrations 011~013 `ON CONFLICT DO NOTHING` |
| 5 | 에스컬레이션 `trigger_type` 엔드포인트 고정 | 8가지 CHECK 제약 (forbidden_hit, prohibited_detected, low_confidence, compound_prohibited, synthetic_flavor, standards_violation, alcohol_boundary, no_data) |
| 6 | `f1_escalation_logs.case_id` ON DELETE CASCADE | 케이스 삭제 시 고아 방지 |
| 7 | `f1_safety_standards` 3-튜플 UNIQUE | `(food_type, standard_type, target_name)` 중복 차단 |

### 리스크 (우선순위)

| # | 리스크 | 심각도 | 위치 | 영향 |
|---|---|---|---|---|
| **1** | `f2_food_type_classification` DDL 부재 (DML만 존재) | 🔴 높음 | 코드 4곳 참조(`feature2.py:88,96`·`preprocess_f2.py:369,500`), DDL은 Node `upload_foodcode5_full.js:192` INSERT뿐 | 환경 동기화 불가(dev/staging/prod), Supabase Studio 수동생성 추정 → **§부록 A** |
| **2** | F1 스켈레톤 10개 미완성 | 🔴 높음 | `combined_schema.sql` (TODO 주석) | 현재 `pipeline_steps.ai_result` JSONB로 우회 |
| **3** | RLS `FORCE ROW LEVEL SECURITY` 미적용 | 🔴 높음 | `009_f1_rls_policies.sql` · 백엔드 `db/supabase_client.py:19-34` **service_role** 사용 | RLS 정책 실질 무력화 (service_role 자동 우회) → **§부록 B** |
| **4** | `feedback_logs.case_id → cases` FK CASCADE 미지정 | 🔴 높음 | `combined_schema.sql:117` | 케이스 삭제 시 **feedback_logs만** 고아 (pipeline_steps·documents·escalation_logs는 이미 CASCADE 적용됨 — 초기 진단 정정) → **§부록 C** |
| **4-B** | `*_by → auth.users(id)` 23개 FK 정책 미지정 | 🟠 중간 | `combined_schema.sql` + `migrations/002·003·004·006·007` | 사용자 탈퇴 시 RESTRICT(기본값)으로 삭제 차단, SET NULL 권장 → **§부록 C** |
| **5** | F2/F4/F5 시드 bootstrap 스크립트 부재 | 🟠 중간 | `backend/scripts/` | seed 수동 실행 의존 → 문제 067·012·013 재발 위험 |
| **6** | `f1_ingredient_synonyms` 정의만·미사용 | 🟡 낮음 | migration 005, 코드 미연동 | 원재료 이명 매칭 미활용 (Step 1-1 강화 여지) |
| **7** | `f1_escalation_logs` INSERT 미구현 | 🟡 낮음 | `services/step1`·`step3` dict 반환만, DB 기록 없음 | HITL 검토 이력 추적 불가 (현재 JSONB 임시 보관) |

---

## 5. 운영 관찰 — migration 011~013 배경

프로젝트 규칙 §7 "seed 파일과 DB 동기화" 절차 부재로 발생한 실제 문제들:

| Migration | 문제 | 현상 | 근본 원인 | 해결 |
|---|---|---|---|---|
| **011** | `f1_forbidden_ingredients` 누락 7건 | seed/04 정의 15건 ≠ 운영 DB 8건 | seed→DB 수동 실행 절차 누락 | 7건 backfill (Step 0 적중률 55.6% → 100%) |
| **012** | `f1_additive_limits` expanded 51건 누락 | seed 06 미적재 | 순차 실행 절차 부재 | 51건 backfill |
| **013** | `f1_safety_standards` expanded 30건 누락 | seed 07 미적재 | 순차 실행 절차 부재 | 30건 backfill |

**교훈**:
- seed 파일은 **정의(source of truth)**, 운영 DB는 **적재 실적(actual)**. 두 지표의 diff 모니터링 필요.
- `scripts/seed_bootstrap.py` 구현 권장 (seed/*.sql 순차 멱등 실행).
- 확정 seed 변경 시 반드시 **migration 파일로도 backfill** 기록 (컨벤션 §7).

---

## 6. 코드 ↔ DB 괴리 (동기화 문제)

### 6.1 DDL 없이 DML만 존재 ("스키마 버전관리 밖")

**대상**: `f2_food_type_classification` — 상세는 **§부록 A** 참조

| 항목 | 경로 | 동작 |
|---|---|---|
| SELECT | `routers/feature2.py:88,96` | 주류 필터(category_no=15) + 일반(상위 60) |
| SELECT | `db/feature2/preprocess_f2.py:500` | 재임베딩 시 전체 조회 |
| **INSERT** | `db/feature2/preprocess_f2.py:344-371` | 식품공전 제5장 PDF 파싱 → 배치 |
| **INSERT** | `db/feature2/preprocessing/upload_foodcode5_full.js:192` | Node 스크립트 배치 |
| **DDL** | — | ❌ **존재하지 않음** (`combined_schema.sql`·migrations 전무) |

→ SQL 마이그레이션으로 승격 필요 (MERGE_GUIDE.md:8-15에서 "우리 환경에서 아직 실행되지 않았다면 실행 필요" 안내 중).

### 6.2 스키마 있으나 미사용

| 테이블 | 상태 | 조치 |
|---|---|---|
| `f1_ingredient_synonyms` | migration 005 정의, 시드 0, 코드 조회 없음 | 동의어 시드 + Step 1-1a 활성화 or 제거 결정 |
| `f1_escalation_logs` | migration 007 정의, 코드에서 INSERT 미수행 | HITL 로깅 구현 (Phase 5 §3) |

### 6.3 F1 스켈레톤 (§3 ⚪)

10개 테이블이 `id + created_at`만 존재. 현재는 `pipeline_steps.ai_result` JSONB에 임시 직렬화 저장. 정규화 vs JSONB 유지 결정 필요 (Phase 5 이후).

---

## 7. 권장 Action (우선순위)

### 즉시 (Phase 4-C)
1. **`f2_food_type_classification` 스키마 정의·검증** (아람) — 리스크 1
2. **RLS `FORCE` 적용** (인프라) — 리스크 3
   - bootstrap 스크립트는 별도 롤 또는 `DISABLE ROW LEVEL SECURITY` 블록 사용

### 단기 (Phase 5 HITL)
3. **F1 스켈레톤 10개 정의 완료** (병찬) — 리스크 2
4. **`f1_escalation_logs` INSERT 구현** (F1) — 리스크 7 · Phase 5 HITL 요구사항
5. **FK CASCADE 일관성 마이그레이션** — 리스크 4·4-B
   - 🔴 `feedback_logs.case_id → cases` — `ON DELETE CASCADE` 추가 (유일 누락)
   - 🟠 `cases.parent_case_id` + `*_by → auth.users` 23개 FK — `ON DELETE SET NULL` 일괄 적용
   - (주의) `pipeline_steps.case_id`·`documents.case_id`·`f1_escalation_logs.case_id`·`case_label_images.*`는 **이미 CASCADE 적용 완료**

### 중기 (Phase 6+)
6. **`scripts/seed_bootstrap.py` 구현** — 리스크 5 (미래 문제 예방)
7. **F2/F4/F5 초기 시드 적재** — 담당자별 작성 가이드
8. **`f1_ingredient_synonyms` 활성화 or 제거 결정** — 리스크 6

---

## 8. 검증 체크리스트 (운영 적용용)

```sql
-- [1] 스키마 없이 참조되는 테이블 감지
SELECT tablename FROM pg_tables
 WHERE schemaname='public' AND tablename='f2_food_type_classification';
-- → 0 rows면 리스크 1 확정

-- [2] seed 대비 실적 diff (F1 forbidden)
SELECT COUNT(*) FROM f1_forbidden_ingredients;   -- 기대: 15

-- [3] F1 5개 핵심 테이블 실적
SELECT 'allowed'    AS t, COUNT(*) FROM f1_allowed_ingredients   UNION ALL
SELECT 'forbidden',    COUNT(*) FROM f1_forbidden_ingredients UNION ALL
SELECT 'additive',     COUNT(*) FROM f1_additive_limits        UNION ALL
SELECT 'safety',       COUNT(*) FROM f1_safety_standards       UNION ALL
SELECT 'escalation',   COUNT(*) FROM f1_escalation_logs;
-- 기대: 110 / 15 / 64 / 40 / (런타임)

-- [4] RLS FORCE 상태
SELECT relname, relforcerowsecurity
  FROM pg_class
 WHERE relname LIKE 'f1_%' AND relkind='r';
-- 모두 false면 리스크 3 확정

-- [5] FK CASCADE/SET NULL 전수 검사
SELECT tc.table_name, kcu.column_name,
       ccu.table_name AS ref_table, ccu.column_name AS ref_col,
       CASE pgc.confdeltype
         WHEN 'a' THEN 'NO ACTION'
         WHEN 'r' THEN 'RESTRICT'
         WHEN 'c' THEN 'CASCADE'
         WHEN 'n' THEN 'SET NULL'
         WHEN 'd' THEN 'SET DEFAULT'
       END AS on_delete
  FROM information_schema.table_constraints tc
  JOIN information_schema.key_column_usage kcu USING (constraint_schema, constraint_name)
  JOIN information_schema.constraint_column_usage ccu USING (constraint_schema, constraint_name)
  JOIN pg_constraint pgc ON pgc.conname = tc.constraint_name
 WHERE tc.constraint_type='FOREIGN KEY' AND tc.table_schema='public'
 ORDER BY tc.table_name, kcu.column_name;
-- feedback_logs.case_id에 on_delete='NO ACTION'이면 리스크 4 확정
```

---

## 부록 A. `f2_food_type_classification` 심층 추적

### A.1 코드 참조 맵

| 위치 | 동작 | 컬럼 | 추정 빈도 |
|---|---|---|---|
| `routers/feature2.py:88` | SELECT (주류 필터) | `category_no, category_name, type_name, definition` | 주류 식품 판정 시 |
| `routers/feature2.py:96` | SELECT (일반, top 60) | 동일 | 일반 식품 분류 시 |
| `db/feature2/preprocess_f2.py:369` | INSERT (배치) | `category_no, category_name, type_name, definition, category_definition, law_source` | 초기 1회 or 재임베딩 시 |
| `db/feature2/preprocess_f2.py:500` | SELECT (재임베딩) | `*` | 전체 Pinecone 재업로드 시 |
| `db/feature2/preprocessing/upload_foodcode5_full.js:192` | INSERT (Node) | 동일 | Node 경로 전처리 |

### A.2 데이터 원천

- **원본**: 식품공전 제5장 PDF 20개 (`1. 과자류, 빵류 또는 떡류.pdf` ~ `20. 기타 식품류.pdf`)
- **행수**: 약 235행 (f2_node_to_py_map.md 명시)
- **파싱**: `preprocess_f2.py:265` — 대/중/소분류 전수 추출 → `{category_no, category_name, category_definition, type_name, definition, law_source='식품공전 제5장'}`

### A.3 DDL 부재 영향

| 문제 | 영향 |
|---|---|
| dev/staging/prod 환경 동기화 | ❌ 새 환경에서 수동 생성 필요 |
| `bootstrap_f1_db.py` 포함 여부 | ❌ 미포함 |
| 컬럼 타입/제약 문서화 | ❌ Python dict 구조에서 역산해야 함 |
| 프로젝트 컨벤션 §7 준수 | ❌ 위반 (f2_ 도메인 전용 변경은 담당자 PR 필수, 이 경우 DDL 자체가 없음) |

### A.4 런타임 동작 예측

`feature2.py:409`에 `except Exception` 블록이 있어 조용한 실패는 없음. 테이블 부재 시:
- supabase-py → PostgrestAPIError
- exception handler → `pipeline_steps.status='error'` + 500 응답
- 사용자는 명확한 에러 확인 가능 (비즈니스 로직 에러 vs "relation does not exist" 구분은 어려움)

### A.5 권장 조치

1. **즉시**: Supabase SQL Editor에서 존재 확인
   ```sql
   SELECT column_name, data_type, is_nullable
     FROM information_schema.columns
    WHERE table_schema='public' AND table_name='f2_food_type_classification';
   ```
2. **단기**: `migrations/014_f2_food_type_classification.sql` 작성 — 실제 스키마 덤프 기반
3. **중기**: `bootstrap_f1_db.py`에 포함 (또는 `bootstrap_db.py`로 범용화)

---

## 부록 B. RLS FORCE 미적용 영향

### B.1 정책 인벤토리 (009_f1_rls_policies.sql)

| 테이블 | ENABLE | FORCE | SELECT | INSERT | UPDATE | DELETE | 시드 `created_by` |
|---|---|---|---|---|---|---|---|
| `f1_allowed_ingredients` | ✅ | ❌ | `true` | `created_by=auth.uid()` | `created_by=auth.uid()` | `created_by=auth.uid()` | NULL |
| `f1_additive_limits` | ✅ | ❌ | `true` | `created_by=auth.uid()` | `created_by=auth.uid()` | `created_by=auth.uid()` | NULL |
| `f1_safety_standards` | ✅ | ❌ | `true` | `created_by=auth.uid()` | `created_by=auth.uid()` | `created_by=auth.uid()` | NULL |
| `f1_forbidden_ingredients` | ✅ | ❌ | `true` | `created_by=auth.uid()` | `created_by=auth.uid()` | `created_by=auth.uid()` | NULL |
| `f1_escalation_logs` | ✅ | ❌ | `true` | `true` | `resolved_by IS NULL OR resolved_by=auth.uid()` | — | NULL |
| `f1_law_chunks` | ✅ | ❌ | `true` | `auth.role()='service_role'` | — | — | — |

### B.2 인증 경로

| 경로 | 키 | RLS 적용 | 증거 |
|---|---|---|---|
| 백엔드 서비스/라우터 전체 | **service_role** | ❌ 자동 우회 | `db/supabase_client.py:19-34` |
| 프론트엔드 | anon | ✅ 적용 | `frontend/lib/supabase.ts:3-10` |
| `routers/db_manager.py:274-308` (admin API) | service_role + `X-User-Id` 헤더 | ❌ 우회, 백엔드에서 수동 `created_by` 세팅 | JWT 검증 미구현 — 헤더 위변조 위험 |
| `scripts/bootstrap_f1_db.py:42-45` | 직접 SQL (`asyncpg`) | ❌ 우회(의도) | 초기화 작업이므로 허용 |

### B.3 실제 노출 시나리오

| 시나리오 | 발생 가능성 | 피해 범위 |
|---|---|---|
| **A. SUPABASE_SERVICE_KEY 탈취** (.env·CI 로그·배포 서버) | 현실적 | 모든 f1_* 테이블 변조 가능 (금지성분 제거, 기준치 완화 등) |
| **B. 백엔드 SQL/쿼리 버그** (`WHERE` 누락) | 중간 | RLS가 service_role 우회 → DB 전체 오염 가능 |
| **C. `X-User-Id` 헤더 위변조** | 낮음 (라우터 차원 문제) | admin API 통해 타 사용자 데이터 수정 가능 |
| **D. 일반 사용자 RLS 우회 시도** (anon) | 매우 낮음 | 현재 프론트가 f1_*에 직접 접근 안 함 |

### B.4 FORCE 적용 영향

| 항목 | FORCE 적용 후 |
|---|---|
| 프론트 anon 접근 | 변화 없음 (이미 RLS 적용 상태) |
| 백엔드 service_role 조회 | **RLS 정책 따름** → `created_by=auth.uid()`는 `NULL=NULL`로 거짓 → UPDATE 실패 가능 |
| `bootstrap_f1_db.py` | `asyncpg` 직접 연결(DB 슈퍼유저)이므로 **영향 없음** (FORCE는 DB 롤만 차단, 슈퍼유저는 별개) |
| Seed 재적재 | 동일한 이유로 영향 없음 (bootstrap 경로 이용) |

### B.5 권장 조치

1. **즉시**: `ALTER TABLE f1_* FORCE ROW LEVEL SECURITY` (5개 테이블)
2. **단기**: `routers/db_manager.py`에 Supabase JWT 검증 미들웨어 추가 (`X-User-Id` 헤더 대체)
3. **중기**: 백엔드 runtime 쿼리는 anon + RPC 함수로 이전, service_role은 bootstrap/admin-only

---

## 부록 C. FK 전수 인벤토리 (30개)

### C.1 cases 참조 (부모: cases)

| 자식.컬럼 | ON DELETE | 판정 | 출처 |
|---|---|---|---|
| `documents.case_id` | CASCADE | ✅ | `combined_schema.sql:73` |
| `pipeline_steps.case_id` | CASCADE | ✅ | `combined_schema.sql:87` |
| `f1_escalation_logs.case_id` | CASCADE | ✅ | `migrations/007:11` |
| `case_label_images.case_id` | CASCADE | ✅ | `add_case_label_images.sql:13` |
| **`feedback_logs.case_id`** | **미지정** | 🔴 | `combined_schema.sql:117` |
| `cases.parent_case_id` | 미지정 | 🟠 | `combined_schema.sql:62` |

### C.2 auth.users 참조 (23개, 모두 미지정)

**공통(5)**: `cases.created_by/locked_by`, `pipeline_steps.edited_by`, `law_alerts.file_uploaded_by`, `feedback_logs.user_id`

**F1(7)**: `f1_allowed_ingredients.created_by`, `f1_additive_limits.verified_by/created_by`, `f1_safety_standards.verified_by/created_by`, `f1_forbidden_ingredients.created_by`, `f1_escalation_logs.resolved_by`

**F5(9)**: `f5_label_rules.created_by`, `f5_allergy_list.created_by`, `f5_additive_label_rules.created_by`, `f5_gmo_ingredients.created_by`, `f5_thresholds.verified_by/created_by`, `f5_ingredient_list.created_by`, `f5_required_documents.created_by`

→ 전원 `ON DELETE SET NULL` 권장 (감사·기준 데이터는 사용자 탈퇴 후에도 유지).

### C.3 기타

| 자식.컬럼 | 부모 | ON DELETE | 판정 |
|---|---|---|---|
| `case_label_images.source_document_id` | `documents(id)` | CASCADE | ✅ |
| `f4_prohibited_expressions.law_document_id` | `f4_law_documents(id)` | 미지정 | 🟡 (SET NULL 권장) |

### C.4 마이그레이션 초안 (migrations/014_fk_hardening.sql)

```sql
BEGIN;

-- 🔴 유일 긴급: feedback_logs.case_id CASCADE
ALTER TABLE feedback_logs DROP CONSTRAINT IF EXISTS feedback_logs_case_id_fkey;
ALTER TABLE feedback_logs
  ADD CONSTRAINT feedback_logs_case_id_fkey
  FOREIGN KEY (case_id) REFERENCES cases(id) ON DELETE CASCADE;

-- 🟠 cases 자기참조 SET NULL
ALTER TABLE cases DROP CONSTRAINT IF EXISTS cases_parent_case_id_fkey;
ALTER TABLE cases
  ADD CONSTRAINT cases_parent_case_id_fkey
  FOREIGN KEY (parent_case_id) REFERENCES cases(id) ON DELETE SET NULL;

-- 🟠 auth.users FK 23개 SET NULL (cases.created_by는 NOT NULL 제약 먼저 해제)
ALTER TABLE cases ALTER COLUMN created_by DROP NOT NULL;
ALTER TABLE cases DROP CONSTRAINT IF EXISTS cases_created_by_fkey;
ALTER TABLE cases
  ADD CONSTRAINT cases_created_by_fkey
  FOREIGN KEY (created_by) REFERENCES auth.users(id) ON DELETE SET NULL;

-- (cases.locked_by, pipeline_steps.edited_by, law_alerts.file_uploaded_by,
--  feedback_logs.user_id, f1_*.created_by/verified_by/resolved_by,
--  f5_*.created_by/verified_by 전원 동일 패턴 반복 — 총 22개 ALTER 추가)

-- 🟡 f4_prohibited_expressions.law_document_id SET NULL
ALTER TABLE f4_prohibited_expressions
  DROP CONSTRAINT IF EXISTS f4_prohibited_expressions_law_document_id_fkey;
ALTER TABLE f4_prohibited_expressions
  ADD CONSTRAINT f4_prohibited_expressions_law_document_id_fkey
  FOREIGN KEY (law_document_id) REFERENCES f4_law_documents(id) ON DELETE SET NULL;

COMMIT;
```

### C.5 삭제 기능 실존 여부

- 프론트엔드: 케이스 삭제 UI 없음 (grep 결과)
- 백엔드: `routers/db_manager.py`의 admin DELETE 경로 존재 가능 (확인 필요)
- Supabase Studio: 직접 DELETE 가능 → FK 제약은 **필수**

---

## 9. 변경 이력

| 날짜 | 작성자 | 내용 |
|---|---|---|
| 2026-04-18 | Claude (조사) | 초판 — 운영·무결성 관점 분석, schema_overview.md와 분리 |
| 2026-04-18 | Claude (심층) | 리스크 1·3·4 심층조사 반영: (1) 리스크 4 정정 — `pipeline_steps`는 이미 CASCADE, `feedback_logs`만 누락 (2) 리스크 4-B 신설 — `*_by → auth.users` 23개 FK (3) 부록 A/B/C 추가 |
