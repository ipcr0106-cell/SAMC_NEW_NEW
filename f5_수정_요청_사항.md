# F5 (세연) 수정 요청 사항

> F5 병합 과정에서 발견된 확인/보완 요청입니다.
> 작성일: 2026-04-16 / 최종 업데이트: 2026-04-18

---

## 1. Pinecone 인덱스 확인 (필수)

- 인덱스명: `f5-law-chunks` (dim 1024, Voyage-3 임베딩)
- 시딩 스크립트: `backend/scripts/f5_embed_laws.py`

팀 공용 Supabase 환경에서 Pinecone에 데이터가 올라가 있는지 확인 필요.

---

## 2. .env 키 추가 (필수)

`backend/.env`에:
```
F5_ANTHROPIC_API_KEY=sk-ant-...    # Claude Sonnet (Phase 1/2 교차검증)
F5_PINECONE_API_KEY=pcsk_...       # Pinecone f5-law-chunks
F5_VOYAGE_API_KEY=pa-...           # Voyage-3 임베딩
F5_PINECONE_INDEX=f5-law-chunks    # (선택) 인덱스명 명시
```

> 기존 코드의 `ANTHROPIC_API_KEY`, `PINECONE_API_KEY`, `VOYAGE_API_KEY`는 `F5_` prefix로 변경되었습니다.

---

## ~~3. 프론트엔드 PDF 업로드 UI — f0와 중복~~ (✅ 해결됨, Option A 적용)

> **2026-04-18 업데이트**: `LabelDraftPage.tsx`에서 PDF 업로드 UI가 완전히 제거되었습니다.
> f0 · F1 · F2 · F4 결과를 자동 참조하여 시안을 생성합니다 (Option A).
>
> 현재 UI 진입 시 표시 문구:
> *"f0 서류 업로드 · F1 수입판정 · F2 식품유형 분류 · F4 라벨 검토 결과를 자동으로 참고하여 식품 등의 표시기준에 맞는 한글표시사항을 생성합니다."*

---

## ~~4. 프론트엔드 API 경로 확인~~ (✅ 해결됨)

> `frontend/lib/api.ts`에 `runFeature5()`, `getFeature5()` 함수 추가 완료.
> `LabelDraftPage.tsx`는 `NEXT_PUBLIC_API_URL` 환경변수를 사용하여 직접 fetch 호출.
> `useParams()`로 `caseId` 주입됨.
>
> **주요 엔드포인트 (LabelDraftPage 기준):**
> - `POST /cases/{id}/pipeline/feature/5/run` — SSE 스트리밍 (`stream: true`)
> - `POST /cases/{id}/pipeline/feature/5/law-search` — Pinecone 법령 원문 검색
> - `PATCH /cases/{id}/pipeline/feature/5` — 시안 확정 저장
> - `GET /cases/{id}/pipeline/feature/5/report?format=docx|pdf` — 검토내역서 다운로드

---

## 5. f0 ~ F4 → F5 데이터 파이프라인 — PM이 임의로 구현한 부분

> **주의**: 이 연결은 PM이 병합 과정에서 임의로 작성한 것입니다.

### 변경 내용

`backend/routers/feature5.py`에 파이프라인 자동 연결이 추가되었습니다:

1. **food_type 자동 보충**: body에 food_type이 없으면 F2 결과에서 자동 조회
2. **파이프라인 컨텍스트 주입**: f0~F4 결과를 합쳐서 documents 목록에 추가 → AI가 이전 기능 결과를 참고

**주입되는 컨텍스트:**
```
[f0] 제품명, 원산지, OEM 여부, 원재료 목록
[F1] 수입판정 결과 (verdict)
[F2] 식품유형, 주류 여부
[F4] 라벨 검토 확정 지적사항 전체 (텍스트 + 이미지 위반)
```

### F5 출력값 (pipeline_steps step_key='5')

```
pipeline_steps (step_key='5')
├── phase1.items[]          12개 필수항목 법령 대조
│   ├── field               "제품명" / "식품유형" / ...
│   ├── law_ref             "식품 등의 표시기준 제4조"
│   ├── law_requirement     법령 요구사항 텍스트
│   ├── document_value      서류에서 추출한 실제 값
│   ├── status              "pass" | "fail" | "unclear"
│   └── note                판정 근거
├── phase2
│   ├── validation[]        Phase 1 교차검증 (agree | disagree | additional_issue)
│   ├── additional_issues[] AI가 추가 발견한 이슈 (severity: error | warning | info)
│   └── draft               ← 최종 한글시안
│       ├── product_name, food_type, ingredients
│       ├── net_weight, expiry, storage
│       ├── manufacturer, importer
│       ├── allergy, gmo, country_of_origin
└── confirmed_by            담당자 확정 시 이름
```

### F4 결과 조회 방식

> F5가 F4 결과를 `f4_results` 테이블에서 직접 조회 (`_fetch_f4_result()` 함수).
> `pipeline_steps step_key="B"` 방식은 사용하지 않음.

### 수정하고 싶다면

`backend/routers/feature5.py`에서 PM이 추가한 함수:

```
def _fetch_pipeline_result(case_id, step_key) → dict   : pipeline_steps 조회
def _fetch_f4_result(case_id) → dict                   : f4_results 테이블 조회
def _enrich_food_type(case_id, body_food_type) → str   : F2 food_type 자동 가져오기
def _build_context_from_pipeline(case_id) → str        : ⭐ 파이프라인 컨텍스트 구성
```

**완전히 걷어내고 싶다면:**
1. 위 함수들 삭제
2. `run_pipeline`에서 `_enrich_food_type` + `_build_context_from_pipeline` 호출 제거
3. 기존 세연 원본과 동일하게 동작 (documents.parsed_md만 사용)

---

## 6. voyageai 패키지 설치 확인

`backend/requirements.txt`에 `voyageai` 패키지가 있는지 확인하세요.

```bash
pip install voyageai
```

---

## 7. 더미 데이터로 테스트하는 방법

F0~F4 결과 없이도 F5를 테스트할 수 있도록 **더미 데이터 시드 엔드포인트**가 추가되었습니다.

```bash
# 위스키 샘플 데이터로 F0~F4 결과 삽입
curl -X POST http://localhost:8000/api/v1/cases/{case_id}/pipeline/seed-dummy

# 삭제
curl -X DELETE http://localhost:8000/api/v1/cases/{case_id}/pipeline/seed-dummy
```

또는 프론트엔드 케이스 상세 페이지 상단의 **"더미 데이터"** 버튼을 클릭하면 됩니다.

---

## 8. 현재 LabelDraftPage 구현 현황 (2026-04-18 기준)

| 기능 | 상태 |
|------|------|
| PDF 업로드 UI 제거 (Option A) | ✅ |
| `useParams()`로 caseId 주입 | ✅ |
| SSE 스트리밍 (phase1_done / done) | ✅ |
| food_type 선택 입력 (F2 결과 자동 fallback) | ✅ |
| 법령 원문 모달 (Pinecone law-search) | ✅ |
| 시안 직접 편집 + 원본 복원 | ✅ |
| 항목 필터 (부적합 / 확인필요 / 불일치) | ✅ |
| 담당자 확정 (PATCH) | ✅ |
| 검토내역서 DOCX / PDF 다운로드 | ✅ |
| `NEXT_PUBLIC_API_URL` 환경변수 사용 | ✅ |

---

## [공통] 법령 DB 전처리 — HWPX/PDF 파일 처리 확인

법령 원본 파일이 HWPX(한글) 또는 PDF 형식으로 제공됩니다.

**현재 상태:**
- f0: PDF(PyMuPDF) + HWPX(외부 파서 서비스) 지원
- F4: PDF(PyMuPDF) + HWPX 지원 (`preprocess_laws.py`)
- **F5**: `backend/scripts/f5_embed_laws.py` — 파일 형식 지원 여부 세연님 확인 필요
