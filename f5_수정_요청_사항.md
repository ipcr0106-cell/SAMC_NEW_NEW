# F5 (세연) 수정 요청 사항

> F5 병합 과정에서 발견된 확인/보완 요청입니다.
> 작성일: 2026-04-16 / 최종 업데이트: 2026-04-16

---

## 1. Pinecone 인덱스 확인 (필수)

- 인덱스명: `f5-law-chunks` (dim 1024, Voyage-3)
- 시딩 스크립트: `backend/scripts/f5_embed_laws.py`

우리 환경에서 Pinecone에 데이터가 올라가 있는지 확인 필요.

---

## 2. .env 키 추가 (필수)

`backend/.env`에:
```
F5_ANTHROPIC_API_KEY=sk-ant-...    # Claude Sonnet 4 (Phase 1/2)
F5_PINECONE_API_KEY=pcsk_...       # Pinecone f5-law-chunks
F5_VOYAGE_API_KEY=pa-...           # Voyage-3 임베딩
```

기존 코드의 `ANTHROPIC_API_KEY`, `PINECONE_API_KEY`, `VOYAGE_API_KEY`는 `F5_` prefix로 변경되었습니다.

---

## 3. 프론트엔드 PDF 업로드 UI — f0와 중복

현재 `LabelDraftPage.tsx`에 PDF 업로드 기능이 내장되어 있습니다. 하지만 우리 파이프라인에서는 **f0에서 이미 서류 업로드+OCR이 완료된 상태**에서 F5가 시작됩니다.

**선택지:**
- A) PDF 업로드 UI 제거 → "시안 생성 실행" 버튼만 남김 (f0 OCR 결과 자동 사용)
- B) PDF 업로드 유지 → 사용자가 추가 서류를 올릴 수 있도록 (현재 상태)

F5는 파이프라인 마지막 단계이므로, 이전 단계 결과가 부족할 때 추가 서류를 올릴 수 있는 B도 나쁘지 않습니다. 세연님이 판단해주세요.

---

## ~~4. 프론트엔드 API 경로 확인~~ (해결됨)

> ~~현재 LabelDraftPage가 호출하는 API 경로 확인~~
>
> **PM 메모**: `frontend/lib/api.ts`에 `runFeature5()`, `getFeature5()` 함수가 추가되었습니다.
> `services/apiClient.ts`의 BASE_URL도 `/api/v1`을 포함하도록 수정되어 경로 불일치 문제가 해결되었습니다.
>
> 다만 `LabelDraftPage.tsx`가 `apiClient`를 직접 사용하는 부분은 `lib/api.ts`의 함수로 교체하는 것을 권장합니다.

---

## 5. f0 ~ F4 → F5 데이터 파이프라인 — PM이 임의로 구현한 부분

> **주의**: 이 연결은 PM이 병합 과정에서 임의로 작성한 것입니다.

### 변경 내용

`backend/routers/feature5.py`에 파이프라인 자동 연결이 추가되었습니다:

1. **food_type 자동 보충**: body에 food_type이 없으면 F2 결과에서 자동 조회
2. **파이프라인 컨텍스트 주입**: f0~F4 결과를 합쳐서 documents 목록에 추가 → AI가 이전 기능 결과를 참고

**주입되는 컨텍스트:**
```
[f0] 제품명, 원산지, OEM 여부, 원재료 목록
[F1] 수입판정 결과 (verdict)
[F2] 식품유형, 주류 여부
[F4] 라벨 검토 확정 지적사항 전체 (텍스트 + 이미지 위반)
```

### ~~F5 출력값 (pipeline_steps step_key='6')~~ → step_key='5'로 변경됨

> **PM 메모**: step_key가 `"6"` → `"5"`로 변경되었습니다. 코드에는 이미 반영됨.

```
pipeline_steps (step_key='5')
├── phase1.items[]          12개 필수항목 법령 대조
│   ├── field               "제품명" / "식품유형" / ...
│   ├── law_ref             "식품 등의 표시기준 제4조"
│   ├── status              "pass" | "fail" | "unclear"
│   └── note                판정 근거
├── phase2
│   ├── validation[]        Phase 1 교차검증
│   ├── additional_issues[] 추가 이슈
│   └── draft               ← 최종 한글시안
│       ├── product_name, food_type, ingredients
│       ├── net_weight, expiry, storage
│       ├── manufacturer, importer
│       ├── allergy, gmo, country_of_origin
└── confirmed_by            담당자 확정 시 이름
```

### F4 결과 조회 방식 변경됨

> **PM 메모**: F5가 F4 결과를 가져오는 방식이 변경되었습니다.
>
> - **이전**: `pipeline_steps`에서 `step_key="B"`로 조회 (F4가 pipeline_steps에 저장하지 않아서 항상 None)
> - **현재**: `f4_results` 테이블에서 직접 조회 (`_fetch_f4_result()` 함수)
>
> 코드에는 이미 반영되어 있으므로 별도 작업 불필요.

### 수정하고 싶다면

`backend/routers/feature5.py`에서 PM이 추가한 함수:

```
def _fetch_pipeline_result(case_id, step_key) → dict
  └─ pipeline_steps 조회

def _fetch_f4_result(case_id) → dict         ← 신규 추가
  └─ f4_results 테이블에서 F4 결과 조회

def _enrich_food_type(case_id, body_food_type) → str
  └─ F2에서 food_type 자동 가져오기

def _build_context_from_pipeline(case_id) → str
  └─ ⭐ 파이프라인 컨텍스트 구성. 바꾸고 싶으면 이 함수를 수정
```

**완전히 걷어내고 싶다면:**
1. 위 함수들 삭제
2. `run_pipeline`에서 `_enrich_food_type` 호출 + `_build_context_from_pipeline` 호출 제거
3. 기존 세연 원본과 동일하게 동작 (documents.parsed_md만 사용)

---

## 6. voyageai 패키지 설치 확인

`backend/requirements.txt`에 `voyageai` 패키지가 있는지 확인하세요. F5의 Pinecone RAG에서 Voyage-3 임베딩을 사용합니다.

```
pip install voyageai
```

---

## 7. (신규) 더미 데이터로 테스트하는 방법

F0~F4 결과 없이도 F5를 테스트할 수 있도록 **더미 데이터 시드 엔드포인트**가 추가되었습니다.

```bash
# 위스키 샘플 데이터로 F0~F4 결과 삽입
curl -X POST http://localhost:8000/api/v1/cases/{case_id}/pipeline/seed-dummy

# 삭제
curl -X DELETE http://localhost:8000/api/v1/cases/{case_id}/pipeline/seed-dummy
```

또는 프론트엔드 케이스 상세 페이지 상단의 **"더미 데이터"** 버튼을 클릭하면 됩니다.

시드 후 F5 `/run`을 실행하면 f0~F4 컨텍스트가 정상적으로 주입됩니다.

---

## [공통] 법령 DB 전처리 — HWPX/PDF 파일 처리 확인

법령 원본 파일이 HWPX(한글) 또는 PDF 형식으로 제공됩니다. 자기 기능의 법령 DB 전처리 스크립트가 **두 형식 모두 처리 가능한지** 확인해주세요.

**확인 사항:**
- PDF 파싱: `pdfplumber`, `PyMuPDF(fitz)`, `pdfminer` 등 사용 가능한지
- HWPX 파싱: `python-hwp`, 외부 파서 서비스, 또는 직접 XML 파싱 가능한지
- 전처리 스크립트에서 파일 확장자에 따라 분기 처리가 되어 있는지
- 법령 업데이트 시스템(`/admin/law-update`)에서 PDF/HWPX 업로드 시 자기 기능의 전처리가 정상 트리거되는지

**현재 상태:**
- f0: PDF(PyMuPDF) + HWPX(외부 파서 서비스) 지원
- F4: PDF(PyMuPDF) + HWPX 지원 (`preprocess_laws.py`)
- F1/F2/F3/F5: 각 담당자가 확인 필요
