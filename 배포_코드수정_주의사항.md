# 배포 시 코드 수정 주의사항

Vercel(프론트엔드) / Railway(백엔드) 배포 과정에서 발생했던 오류와 예방 규칙을 정리합니다.

---

## 1. TypeScript 타입 불일치 (가장 빈번)

### 문제
같은 이름의 interface/type이 여러 파일에 중복 정의되어 있을 때, 한쪽만 수정하면 빌드 실패.

### 실제 사례
| 파일 A | 파일 B | 문제 |
|--------|--------|------|
| `page.tsx`의 `ParsedData` | `OcrResultEditor.tsx`의 `ParsedData` | `ProcessCodeReason`에 `name` 필드 누락 |
| `page.tsx`의 `process_code_candidates` | `ProcessCodeCard.tsx`의 `ProcessCodeCandidate` | `name`, `confusion_note` required vs optional 불일치 |

### 예방 규칙
- **타입을 수정할 때는 반드시 해당 타입이 사용되는 모든 파일을 검색**할 것
  ```bash
  # 예: ProcessCodeReason 타입을 수정할 때
  grep -r "ProcessCodeReason" frontend/
  ```
- 같은 타입이 여러 곳에 정의되어 있으면 **모든 곳을 동일하게 수정**
- 가능하면 타입을 한 곳(`types/` 폴더)에 정의하고 import해서 사용

---

## 2. 상수 객체에 누락된 키

### 문제
status 등 enum-like 값에 새 항목을 추가했는데, 관련 상수 객체(라벨, 색상 등)에는 추가하지 않으면 빌드 실패.

### 실제 사례
```typescript
// StandardCheck의 status에 "review_needed" | "no_data" 추가했지만
// STANDARDS_STATUS_COLOR / STANDARDS_STATUS_LABEL에는 빠져있었음
```

### 예방 규칙
- status, type 등 **새 값을 추가하면 관련 매핑 객체도 반드시 업데이트**
  ```bash
  # 해당 상수명으로 검색
  grep -r "STANDARDS_STATUS" frontend/
  ```
- `as const`로 정의된 객체는 TypeScript가 인덱싱 오류를 잡아줌 → 이 에러가 나면 키 누락 의심

---

## 3. 로컬에서 빌드 확인 후 push

### 규칙
코드 수정 후 push 전에 반드시 로컬 빌드 테스트:
```bash
cd frontend
npx next build
```
- `Compiled successfully` → OK
- `Failed to compile` → 에러 수정 후 push

### 주의
- `npm warn deprecated ...` 메시지는 **경고일 뿐 에러가 아님** → 무시해도 됨
- 실제 에러는 `Failed to compile`, `Type error`, `Error` 키워드로 시작

---

## 4. tsconfig.json exclude 설정

### 문제
테스트 설정 파일(`vitest.config.ts`)이 빌드 대상에 포함되면 타입 에러 발생.

### 현재 설정 (유지할 것)
```json
// frontend/tsconfig.json
"exclude": [
  "node_modules",
  "vitest.config.ts",
  "vitest.setup.ts"
]
```

### 예방 규칙
- 테스트 전용 파일을 추가할 때는 `tsconfig.json`의 `exclude`에 포함되어 있는지 확인
- `*.test.ts`, `*.spec.ts` 파일은 Next.js가 자동으로 제외하므로 괜찮음

---

## 5. 환경변수 관련

### 프론트엔드 (Vercel)
| 변수 | 용도 | 주의 |
|------|------|------|
| `NEXT_PUBLIC_API_URL` | 백엔드 API 주소 | 배포 시 Railway URL로 변경 필요 |
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 주소 | 변경 금지 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase 공개키 | 변경 금지 |
| `F3_OPENAI_API_KEY` | F3 서버사이드 AI | 서버사이드 전용, `NEXT_PUBLIC_` 붙이면 안 됨 |

### 백엔드 (Railway)
- `.env` 파일의 모든 키를 Railway Variables에 등록해야 함
- `.env` 파일은 **절대 Git에 커밋하지 말 것** (`.gitignore`에 등록됨)

### 예방 규칙
- `NEXT_PUBLIC_` prefix가 붙은 변수는 클라이언트에 노출됨 → **시크릿 키에 절대 사용 금지**
- 환경변수 변경 후 Vercel에서 **Redeploy** 해야 반영됨 (자동 반영 안 됨)

---

## 6. 백엔드 CORS 설정

### 현재 설정 (main.py)
```python
allow_origins=["*"]  # 모든 도메인 허용
```
- 1차 배포에서는 이대로 사용
- 프로덕션 배포 시에는 특정 도메인만 허용하도록 변경 권장

---

## 7. 배포 브랜치

| 서비스 | 배포 브랜치 | 설정 위치 |
|--------|------------|-----------|
| Vercel (프론트엔드) | `develope` | Settings → Environments → Production |
| Railway (백엔드) | `develope` | Settings → Branch connected to production |

- `develope` 브랜치에 push/merge하면 자동 배포 트리거
- **main 브랜치에 push해도 배포되지 않음**

---

## 요약 체크리스트

코드 수정 후 push 전에 확인:

- [ ] 타입 수정 시 → 해당 타입이 사용되는 모든 파일 검색 & 동기화
- [ ] 새 status/type 값 추가 시 → 관련 라벨/색상 상수 업데이트
- [ ] `cd frontend && npx next build` 로컬 빌드 성공 확인
- [ ] `.env` 파일이 커밋에 포함되지 않는지 확인
- [ ] 환경변수 변경 시 → Vercel/Railway에서도 동일하게 변경
