# F4 (성은) 수정 요청 사항

> F1/F2 병합 과정에서 F4에 추가된 파이프라인 연결 안내입니다.
> 작성일: 2026-04-16 / 최종 업데이트: 2026-04-16

---

## 1. F1/F2 → F4 자동 연결 구현됨 — PM이 임의로 구현한 부분

> **주의**: 이 연결은 PM이 병합 과정에서 임의로 작성한 것입니다.
> F4 담당자가 검토 후 마음에 안 들면 자유롭게 수정하세요.

### 변경 내용

`backend/routers/feature4.py`의 `_run_analysis()` 함수에 F1/F2 자동 보충 로직이 추가되었습니다.

**기존**: `food_type`과 `ingredients`를 프론트에서 직접 전달해야 했음 (기본값: `"미분류"`, `[]`)
**변경**: 값이 비어있으면 pipeline_steps에서 F2/F1 결과를 자동 조회

```
F2 (step_key='2') → food_type 자동 보충
  - req.food_type이 "미분류"이면 → F2 결과에서 food_type 가져옴

F1 (step_key='1') → ingredients 자동 보충
  - req.ingredients가 비어있으면 → F1 결과에서 원재료 이름 목록 가져옴
```

### F4의 전체 데이터 파이프라인 (현재 상태)

```
f0 (step_key='0') ─── 연결됨 (기존)
├── product_name      → _fetch_doc_ocr()
├── ingredients       → _fetch_doc_ocr()
├── export_country    → _fetch_doc_ocr()
├── label_texts       → _fetch_label_ocr()
├── label_images      → case_label_images 테이블
├── content_volume    → ❌ 미연결 ("" 하드코딩, f0에 필드 추가 필요)
└── manufacturer      → ❌ 미연결 ("" 하드코딩, f0에 필드 추가 필요)

F1 (step_key='1') ─── 연결됨 (PM 추가)
└── ingredients[].name → req.ingredients 자동 보충

F2 (step_key='2') ─── 연결됨 (PM 추가)
└── food_type         → req.food_type 자동 보충
```

### 수정하고 싶다면

PM이 추가한 코드는 `backend/routers/feature4.py`에 있는 함수 2개입니다:

```
def _fetch_f1_result(supabase, case_id) → dict
  └─ pipeline_steps(step_key='1')에서 F1 결과 조회

def _fetch_f2_result(supabase, case_id) → dict
  └─ pipeline_steps(step_key='2')에서 F2 결과 조회
```

호출부는 `_run_analysis()` 함수 안:
```python
# F2 → food_type 자동 보충
if req.food_type == "미분류":
    f2 = _fetch_f2_result(sb, case_id)
    ...

# F1 → ingredients 자동 보충
if not req.ingredients:
    f1 = _fetch_f1_result(sb, case_id)
    ...
```

**완전히 걷어내고 싶다면:**
1. `_fetch_f1_result`, `_fetch_f2_result` 함수 삭제
2. `_run_analysis` 안의 `# F2 → food_type` / `# F1 → ingredients` 블록 삭제
3. 기존 성은 원본과 동일하게 동작 (프론트에서 직접 전달 방식)

---

## 2. (신규) F4 결과 저장 위치 — `f4_results` 테이블 (pipeline_steps 아님)

F4는 다른 Feature와 달리 결과를 `pipeline_steps`가 아닌 **`f4_results`** 별도 테이블에 저장합니다. 이로 인해:

- F5가 F4 결과를 참조할 때 `f4_results` 테이블에서 직접 조회합니다 (이미 수정 완료)
- `f4_results` 테이블에 `case_id`에 대한 unique constraint가 없습니다. 동일 케이스로 여러 번 분석하면 중복 행이 생길 수 있으니 확인 필요.

**권장**: `f4_results`에 `UNIQUE(case_id)` constraint 추가, 또는 다른 Feature처럼 `pipeline_steps`에 `step_key='4'`로 통일.

---

## 3. (신규) `gpt-5.4-nano` 모델명 확인

`backend/routers/feature4.py` line 650, 715에서 `model="gpt-5.4-nano"`를 사용합니다. 이 모델이 사용 가능한 것인지 확인해주세요. 사용 불가능하면 `gpt-4o-mini` 등으로 변경 필요합니다.

---

## 4. (신규) Windows 전용 폰트 경로 하드코딩

`backend/routers/feature4.py` line 1125-1126:
```python
_FONT_PATH = "C:/Windows/Fonts/malgun.ttf"
_FONT_BOLD_PATH = "C:/Windows/Fonts/malgunbd.ttf"
```

Linux/macOS 배포 시 PDF 리포트 생성이 실패합니다. 배포 환경에 따라 분기하거나 폰트를 프로젝트에 번들하는 것을 권장합니다.

**긴급도**: 낮음 (로컬 개발 시에는 문제없음, 배포 시 수정 필요)

---

## 5. (신규) async 엔드포인트에서 동기 블로킹 I/O

`backend/routers/feature4.py`의 `analyze` 엔드포인트가 `async def`이지만 내부에서 동기 함수를 호출합니다:
- OpenAI API 동기 호출
- Pinecone 쿼리 동기 호출
- `SentenceTransformer("intfloat/multilingual-e5-large")` 모델 로딩 (첫 요청 시 수 초 블로킹)
- Supabase SDK 동기 호출

F2와 동일한 문제입니다. `def`로 변경하거나 `asyncio.to_thread()`로 감싸세요.

**긴급도**: 중간

---

## 6. (신규) F4 프론트엔드 — 기존 결과 로드 안 됨

`frontend/app/cases/[id]/f4/page.tsx`에서 `useForeignLabelCheck` 훅이 `fetchResult`를 반환하지만 **호출하지 않습니다**. 페이지 진입 시 이전에 저장된 F4 분석 결과가 표시되지 않습니다.

또한 `frontend/features/feature4/ForeignLabelPage.tsx`는 별도의 완성된 컴포넌트이지만 **어디에서도 import하지 않는 죽은 코드**입니다. `f4/page.tsx`가 인라인으로 구현되어 있어서 중복됩니다.

**수정:** `f4/page.tsx`에서 `useEffect`로 `fetchResult(caseId)` 호출 추가, 또는 `ForeignLabelPage.tsx`를 사용하도록 변경.

---

## [공통] 법령 DB 전처리 — HWPX/PDF 파일 처리 확인

법령 원본 파일이 HWPX(한글) 또는 PDF 형식으로 제공됩니다. 자기 기능의 법령 DB 전처리 스크립트가 **두 형식 모두 처리 가능한지** 확인해주세요.

**확인 사항:**
- PDF 파싱: `pdfplumber`, `PyMuPDF(fitz)`, `pdfminer` 등 사용 가능한지
- HWPX 파싱: `python-hwp`, 외부 파서 서비스, 또는 직접 XML 파싱 가능한지
- 전처리 스크립트에서 파일 확장자에 따라 분기 처리가 되어 있는지
- 법령 업데이트 시스템(`/admin/law-update`)에서 PDF/HWPX 업로드 시 자기 기능의 전처리가 정상 트리거되는지

**현재 상태:**
- f0: PDF(PyMuPDF) + HWPX(외부 파서 서비스) 지원
- F4: PDF(PyMuPDF) + HWPX 지원 (`preprocess_laws.py`)
- F1/F2/F3/F5: 각 담당자가 확인 필요
