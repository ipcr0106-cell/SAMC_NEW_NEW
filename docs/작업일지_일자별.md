# 작업 일지 (일자별)

> 기준: `cks4221224-ops` 계정 커밋 기록
> 범위: 2026-04-16 ~ 2026-04-20

---

## 📅 2026-04-16 (목) — F1 asyncpg → supabase-py 전환 & 매칭 체인 보강

### F1 DB 인프라 전환
- **F1: asyncpg → supabase-py 전환 완료** (`faa3130`)
- `fix`: F1 async→sync 전환, `.env.example` 복원, 미사용 필드 주석 (`2bfc80d`)
- `chore`: `requirements.txt`에서 asyncpg 의존성 제거 (`426375b`)

### F1 매칭 체인 개선
- `fix`: F1 금지원료 부분매칭 보강 + trgm RPC 방어 + 테스트 21개 (`996a465`)
- `test`: F1 매칭 체인 단위 테스트 24개 추가 (45 total, all passed) (`07a8e4d`)
- `refactor`: F1 매칭 체인 1단계에 원재료명 정규화 적용 (`2f8a4e3`)

### F1 기능 확장
- `feat`: F1 레포트 PDF 다운로드 기능 구현 (`27017b1`)
- `test`: Step 3 단위(33개) + 통합(11개) 테스트 추가 (89 total, all passed) (`a6f94d5`)
- `db`: F1 법령 DB 확충 — 첨가물 51건 + 안전기준 31건 + 금지원료 8→15건 (`e6885e5`)

### 문서/리서치
- `docs`: newsamc에서 F1에 적용할 교훈 5개 파일 정리 (`f86a5d5`)
- `docs`: newsamc 교훈 재평가 — 실질 가치 20%, 나머지 이미 구현/과잉 (`f483a8d`)

---

## 📅 2026-04-17 (금) — F1 RAG 전면 구축 (Phase 1~5) & F2 리팩토링

### 환경 설정
- `chore`: `.gitignore` 보강 — Claude/OMC 로컬 상태, TS 빌드 캐시, IDE/보안 패턴 추가 (`23f717d`)

### F2 백엔드/프론트 리팩토링
- `refactor(F2)`: 백엔드 규약 준수 — HTTPException 3-key / async→def / None 방어 (`7410e0d`)
- `refactor(F2-front)`: caseId props 연결 (F3) (`9cba5a1`)
- `refactor(F2-front)`: 파일 업로드 UI 제거 — f0가 담당 (F1) (`b46a371`)
- `refactor(F2-front)`: 분류 결과 단일 화면 재구성 (F4) (`bed805d`)
- `refactor(F2-front)`: API 경로 교체 `/step2` → `runFeature2` (F2) (`9d77c20`)
- `refactor(F2-front)`: Tailwind + f0 레이아웃 적용 (F5) (`3a70db1`)

### F2 전처리 Sprint 1
- `feat(F2-preprocess)`: Phase 3 Sprint 1 — 전처리 스켈레톤 + admin 등록 (`1db0ddc`)

### F1 RAG Phase 1~3 (구축)
- `docs(F1-rag)`: Phase 1~5 구현 계획서 (총괄/백엔드/프론트/전처리/Phase) (`5d4db32`)
- `feat(F1-rag)`: Phase 1 — Pinecone `samc-law-f1` + OpenAI 클라이언트 + 미러 테이블 (`7ba98d1`)
- `feat(F1-rag)`: Phase 2 — newsamc `samc-law` → `samc-law-f1` 484건 복제 완료 (`eb53d79`)
- `feat(F1-rag)`: Phase 3 — 식품첨가물공전 1,664건 임베딩 완료 (`d837a85`)
- `docs(F1-rag)`: Phase 1~3 완료 상태 마킹 + 이어받기 가이드 (`762a24e`)

### F1 RAG Phase 4-A (판정 모듈)
- `feat(F1-rag)`: Phase 4-A — RAG 판정 모듈 + Pydantic 모델 + integration 테스트 (`25592da`)
- `docs(F1-rag)`: Phase 4-A 완료 상태 마킹 (`8489bd9`)

### F1 RAG Phase 4-B (HITL 통합 & 골든셋 평가)
- `feat(F1-rag)`: Phase 4-B-1 — HITL 통합 (`feature1.py` + `routers/feature1.py`) (`79cc128`)
- `chore(F1-rag)`: Phase 4-B-2 — admin_law_update RAG 교체 + law_extractor 폐기 (`6c8c5a7`)
- `test(F1-rag)`: Phase 4-B-3a — 골든셋 스키마 + 5건 시드 (`41a0e94`)
- `test(F1-rag)`: Phase 4-B-3b — 골든셋 25건 추가 + 러너 스크립트 (`3d1eb73`)
- `docs(F1-rag)`: Phase 4-B-3c — 골든셋 평가 리포트 + OPEN-2 결정 (`739eb64`)
- `feat(F1-rag)`: Phase 4-B-3d — SYSTEM_PROMPT few-shot 튜닝 **(70% → 93.3%)** (`5035672`)
- `test(F1-rag)`: Phase 4-B-3e — 골든셋 v2 (100건) + 러너 비용 추정 버그 수정 (`d6495e2`)
- `feat(F1-rag)`: Phase 4-B-3e — SYSTEM_PROMPT few-shot v2 + 프롬프트 모드 토글 + 기본 모델 `gpt-5.4-mini` (`fd3ad6d`)
- `docs(F1-rag)`: Phase 4-B-3e — 모델 비교 리포트 + OPEN-2 재결정 (5.4-mini few v2 채택 71%) (`5212500`)
- `fix(F1)`: Step 0 forbidden 게이트 8건 누락 수정 **(일치율 71% → 76%)** (`c9f0447`)
- `docs(F1-rag)`: Phase 4-B-3e 완료 마킹 — R6 Step 0 fix 후 76.0% 최종 채택 (`6fe75dd`)

### F1 DB 확충 (Stage 1~2)
- `chore(F1-db)`: `f1_additive_limits` / `f1_safety_standards` drift backfill migration 작성 (`e3b4ed4`)
- `docs(F1-rag)`: Phase 4-B-3e §4.5 후속 — Stage 1 DB 보강 완료 주석 (`92a9946`)
- `feat(F1-db)`: Stage 2 — newsamc additive_limits 830건 `f1_additive_limits` 이관 (`c45f4ec`)
- `chore(F1-db)`: newsamc 830건 `is_verified=true` 승격 — 팀 룰 예외 기록 (`875e848`)
- `fix(F1)`: `run_feature1_endpoint` sync 복원 (팀 룰 준수, 옵션 B) (`17ae880`)

### F1 RAG Phase 5 (프론트엔드)
- `feat(F1-rag)`: Phase 5 — 프론트 인용 UI + HITL 패널 + PDF 레포트 + 문서 (`0c381c4`)

### F2 전처리 Sprint 2
- `feat(F2-preprocess)`: Phase 3 Sprint 2 — Node→Python 이관 구현 (`b3e67cc`)
- `fix(F2-preprocess)`: print 한글 제거 — f2-check FAIL 대응 (`85e8d4f`)

---

## 📅 2026-04-18 (토) — 버그 수정

- `fix(F2-preprocess)`: `upload_foodtype_chunks` 테이블명 정정 (`c013ed0`)
- `fix(F1-rag)`: Phase 5 HITL 복구 — `pipeline_steps.status` CHECK 에 `needs_review` 추가 (`303b76c`)
- 04180100커밋 (`b4b34c6`)
- md 삭제 (`6ee676e`)

---

## 📅 2026-04-19 (일) — (개인 커밋 없음)

---

## 📅 2026-04-20 (월) — F1 재설계 Wave 1~4 + F1/F2 통합

### 재설계 시작
- `docs(f1)`: Wave 1 실행 모델 B → A 전환 + 재설계 계획 문서 초기 커밋 (`c4409d8`)

### Wave 1 — data.go.kr 연동 + 단위 정규화
- `feat(f1)`: Wave 1 Day 0 interface skeleton (`5d00068`)
- `W1-B`: Pydantic 모델 확장 + `F1Ingredient` TS 타입 + 단위테스트 (`06d85ff`)
- `W1-D`: 단위 정규화 모듈 구현 (`normalize_to_common_unit`, `parse_numeric_spec`, `SpecEvaluation`) (`b7cbba9`)
- `W1-A`: data.go.kr 4-endpoint client 본체 + tests (`78c1910`)
- `W1-A`: data.go.kr 4-endpoint client body + cache + migration + 52 tests (93% cov) (`c932175`)
- `fix(f1)`: Wave 1 code-review HIGH/MEDIUM 대응 (커넥션 풀 + api_key 마스킹 + F1Output rename) (`0f96426`)
- **PR #23 머지** — `feature/f1-wave1` (`3a6d7a7`)

### Wave 2 — Step A/B/C/D + Orchestrator
- `feat(f1)`: Wave 2 Day 0 interface skeleton (Step A/B/C/D + orchestrator) (`4c5471e`)
- `W2-A`: Step A 금지원료 체크 본체 구현 (`c2ca565`)
- `W2-D`: Step D 법령 인용 서비스 본체 구현 (`f5b44d6`)
- `W2-B`: Step B 원재료 매칭 + 성분코드 + GMO 본체 구현 (`8b3335a`)
- `W2-C`: Step C 기준규격 수치 비교 본체 구현 (53 tests, 87% cov) (`44714b2`)
- `feat(f1)`: Wave 2 orchestrator `run_feature1_v2` + 통합 테스트 I-02~I-07 (`00b808f`)
- `fix(f1)`: Wave 2 code-review CRITICAL/HIGH 대응 (`get_running_loop` + `StepBResult.stopped`) (`49408a8`)
- **PR #24 머지** — `feature/f1-wave2` (`ca32fb7`)

### Wave 3 — HITL 구현 + 골든셋 v3 + CI
- `feat(f1)`: Wave 3 Day 0 interface skeleton (HITL API + migrations + FE components) (`9b4df98`)
- `W3-FE`: HITL 프론트엔드 컴포넌트 구현 및 `RagConflictPanel` 제거 (`09f59ef`)
- `W3-BE`: HITL 백엔드 서비스·엔드포인트·테스트 구현 (`f1f82b6`)
- `W3-QA`: 골든셋 v3 57건 + CI workflow + E2E E-01 스켈레톤 + METRICS.md (`4814fe2`)
- `fix(f1)`: Wave 3 code-review/verifier BLOCKER 3건 대응 (`c2bbfa7`)
- `chore(ci)`: `f1_tests` workflow 을 template 위치로 이동 (workflow scope 우회) (`723ec63`)
- **PR #25 머지** — `feature/f1-wave3` (`319c427`)

### Wave 4 P2 — v2 라우터 와이어링
- `P2-BE`: F1 v2 라우터 와이어링 — feature flag 분기 + `F1Output` 어댑터 + 테스트 (`bf1fd8e`)
- `P2-FE`: `ImportCheckPage` HITL-0/1/2 배선 및 `StatusBadge` 신규 (`73d342f`)
- `fix(f1)`: Wave 4 P2 code-review MEDIUM-1 대응 (HTTPException 원본 전파) (`8413695`)
- **PR #26 머지** — `feature/f1-wave4-p2` (`6678acb`)

### Wave 4 P3/P4 — 자동화 & testid 보완
- `feat(f1)`: Wave 4 P4 자동화 (testid 보완 + Step A 컬럼 fix + 골든셋 full 실행) (`dcfc441`)
- `chore(ci)`: 골든셋 실행 결과 JSON 무시 (baseline 0452 예외 유지) (`1ec5879`)
- **PR #27 머지** — `feature/f1-wave4-p3-p4` (`a4d65ea`)

### F1+F2 경량 통합 (라우팅 6→5 스텝)
- `f1f2`: Sub-phase 2 — F1 페이지에 `FoodTypeSection` + `FoodTypeEditDialog` 통합 (`7954965`)
- `f1f2`: Sub-phase 3/4 — 라우팅 6→5 스텝 축소 + F2 deprecated 마킹 (`58cef4f`)
- **PR #28 머지** — `feature/f1-f2-merge` (`d92a179`)

### F1 Step 어댑터 확장
- `fix(f1)`: v2 어댑터 확장 — Step A/B/C/D UI 렌더용 `_internal` 매핑 추가 (`684bddc`)
- **PR #29 머지** — `feature/f1-step-adapter` (`d0a8b6e`)

### Wave 4 P4 보완 (T1~T3)
- `docs(f1)`: Wave 4 P4 보완 계획서 추가 (`d90116f`)
- `feat(f1)`: F2 실행 트리거 복구 (Wave 4 P4 보완 T1) (`3e11e1a`)
- `feat(f1-wave4-p4)`: T2 SPEC_VAL 파싱 강화 + T3 synonym 매칭 경로 추가 (`2ae275a`)

### 기타 머지/브랜치
- Merge branch `develope` into `BC` (`0b367b3`)
- **PR #32 머지** — `ipcr0106-cell/BC` (`e6390e2`)
- **PR #37 머지** — `ipcr0106-cell/develope` (`757d933`)

---

## 📊 요약 통계

| 날짜 | 커밋 수 | 주요 테마 |
|---|---|---|
| 2026-04-16 | 11 | F1 asyncpg→supabase 전환, 매칭 체인 보강 |
| 2026-04-17 | 34 | F1 RAG Phase 1~5 전면 구축, F2 리팩토링 |
| 2026-04-18 | 4 | 버그 수정 |
| 2026-04-19 | 0 | — |
| 2026-04-20 | 44 | F1 재설계 Wave 1~4, F1+F2 통합, PR #23~29/32/37 머지 |
| **합계** | **93** | — |

### 주요 성과
- ✅ **F1 DB 레이어 전환**: asyncpg → supabase-py 완전 전환
- ✅ **F1 RAG 시스템 구축**: Phase 1~5 (Pinecone + OpenAI, 골든셋 100건, 일치율 76%)
- ✅ **F1 재설계 Wave 1~4 완주**: data.go.kr API + Step A/B/C/D + HITL + CI/골든셋 v3
- ✅ **F1+F2 통합**: 6→5 스텝 라우팅 축소
- ✅ **테스트 커버리지**: Wave 1 93% / Wave 2 Step C 87%
