# F0 로직 설명서

> 작성일: 2026-04-20
> 담당: 경아 (F0)
> 범위: 전체 파이프라인 구조 + F0의 기능 및 중요성

---

## 1. 프로젝트 전체 구조

### 1-1. SAMC가 뭐 하는 서비스인가

SAMC는 **수입식품 통관 검역** 업무를 자동화하는 AI 플랫폼입니다. 수입자가 제품 서류(원재료배합비율표, 제조공정도, MSDS, 수출국 라벨 등)를 업로드하면, 6단계 AI 파이프라인이 순차적으로 검토해 "이 제품이 한국에 수입 가능한가, 어떤 서류가 필요한가, 한글 라벨은 어떻게 만들어야 하는가"까지 답을 주는 시스템입니다.

### 1-2. 6단계 파이프라인

```
[사용자] 서류 업로드
   │
   ▼
 ┌──────────────────────────────────────────────┐
 │ F0 : 입력 + OCR + 파싱  (← 본 문서 대상)     │
 │      모든 서류를 읽어서 구조화된 JSON 생성   │
 └──────────────────────────────────────────────┘
   │  (case_id, ParsedResult)
   │
   ├──► F1 : 수입 가능 판정 (원재료 허용/금지 + 기준치)
   │
   ├──► F2 : 식품유형 분류 (GPT + Pinecone RAG)
   │
   ├──► F3 : 필요서류 안내 (식품유형 + 조건 기반 매칭)
   │
   ├──► F4 : 수출국 라벨 검토 (허위·과장 표시 탐지)
   │
   └──► F5 : 한글 표시사항 시안 (최종 한글 라벨 초안)
```

### 1-3. 데이터 흐름 (누가 무엇을 읽는가)

| 단계 | F0 결과를 어떻게 쓰는가 |
|------|------------------------|
| **F1** | `ingredients` (원재료명/INS/CAS/비율/부위/하위성분) → 허용·금지·기준치 판정. `process_info.process_codes` → 공정 기반 기준치 적용. `basic_info.alcohol_percentage` → 주류 전용 기준치 분기. |
| **F2** | `ingredients` + `process_info` → 식품공전의 유형 매칭. |
| **F3** | `basic_info.export_country`, `is_oem`, F2가 확정한 `food_type` → 필요서류 5축 매칭. |
| **F4** | `label_info.label_texts`, `label_info.design_description`, `case_label_images` 크롭 이미지 → 법령 위반 여부 AI 판정. |
| **F5** | 위 전부 + `basic_info.manufacturer`, `content_volume` → 한글 라벨 시안 생성. |

**요점: F0의 출력 JSON 한 덩어리(`ParsedResult`)가 F1~F5 전원의 입력입니다. F0가 틀리면 그 아래 전부 틀립니다.**

### 1-4. 팀 구성

| 단계 | 담당 | 주요 기술 |
|------|------|----------|
| F0 | 경아 | OpenAI GPT-4o Vision + LLM 파싱, PyMuPDF, PIL |
| F1 | 병찬 | Supabase + Pinecone RAG + gpt-5.4-mini, HITL |
| F2 | 아람 | GPT-4o + Pinecone (samc-a) |
| F3 | 유빈 | 5축 AND 매칭 + Pinecone (samc-law-f3) |
| F4 | 성은 | Vision 분석 + Pinecone (samc-feature4-laws) + DeepL |
| F5 | 세연 | Claude Sonnet SSE + Pinecone (f5-law-chunks) + Voyage-3 |

---

## 2. F0의 기능 및 중요성

### 2-1. F0가 하는 일

한 문장으로: **"다양한 형식의 서류를 읽어서, AI가 소화할 수 있는 깔끔한 JSON 한 덩어리로 변환"**

구체적으로는 4가지 일을 합니다:

1. **파일 업로드 & 저장**
   - 지원 포맷: PDF, PNG/JPG/WebP, HWP/HWPX, XLSX/XLS
   - Supabase Storage(`documents` 버킷)에 저장
   - `documents` 테이블에 메타데이터 기록

2. **OCR (파일 → Raw 텍스트)**
   - PDF: PyMuPDF로 텍스트 추출, 스캔본은 페이지별 Vision OCR
   - 이미지: base64 → OpenAI Vision(`gpt-4o`)로 텍스트 인식
   - HWP/HWPX: 외부 parser-service(kordoc) HTTP 호출
   - Excel: openpyxl로 셀 데이터 수집

3. **LLM 파싱 (Raw 텍스트 → 구조화 JSON)**
   - 4개 문서의 Raw 텍스트를 OpenAI GPT-4o에 한 번에 투입
   - `ParsedResult` 스키마에 정확히 맞는 JSON 반환
   - INS·CAS 번호 추출, 식약처 공정코드 211개 매핑, 복합원재료 하위 성분 재귀 파싱

4. **라벨 이미지 처리 (F4용 사전 작업)**
   - 라벨 사진에서 "제품 사진 영역" bbox 검출 → PIL로 크롭 → PNG 저장
   - 동시에 제품명/원재료/내용량/원산지/제조사/케이스넘버 텍스트 동시 추출
   - MD5 해시로 중복 업로드 감지, `case_label_images` 테이블에 저장

### 2-2. F0의 기술 스택

- **API 라우터**: `backend/routers/upload.py`, `backend/routers/cases.py`
- **서비스**: `backend/services/ocr_service.py`, `parsing_service.py`, `label_image_service.py`, `export_service.py`
- **스키마**: `backend/schemas/upload.py` (DocType, ParseStatus, UploadResponse, IngredientItem, BasicInfo, ProcessInfo, LabelInfo, ParsedResult 등)
- **외부 API**: OpenAI GPT-4o (텍스트 파싱 + Vision), Supabase (DB + Storage)
- **환경변수**: `F0_OPENAI_API_KEY`, `F0_OPENAI_MODEL`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`

### 2-3. F0가 쓰는 DB 테이블

| 테이블 | 구분 | F0 역할 |
|--------|------|--------|
| `cases` | 공통 | 읽기 + `current_step` UPDATE |
| `documents` | 공통 | INSERT/SELECT/DELETE, `parsed_md` UPDATE |
| `pipeline_steps` | 공통 | `step_key='0'`, `step_name='입력 및 OCR 파싱'`으로 UPSERT, `ai_result` JSONB에 ParsedResult 저장 |
| `case_label_images` | F0 전용 | INSERT/SELECT/DELETE (v1+v2+v3 마이그레이션 적용 완료) |

※ F0는 `f0_` prefix 테이블을 별도로 두지 않습니다. 공통 3개 + 무프리픽스 1개만 씁니다.

### 2-4. F0의 엔드포인트

| 메서드 | 경로 | 용도 |
|--------|------|------|
| POST | `/api/v1/cases` | 새 케이스 생성 |
| GET | `/api/v1/cases` | 케이스 목록 조회 |
| GET | `/api/v1/cases/{id}` | 단일 케이스 상세 조회 |
| PUT | `/api/v1/cases/{id}/parsed-result` | OCR 결과 사용자 수정 저장 |
| POST | `/api/v1/cases/{id}/upload` | 서류 파일 업로드 |
| POST | `/api/v1/cases/{id}/parse` | OCR + LLM 파싱 실행 |
| GET | `/api/v1/cases/{id}/documents` | 업로드 문서 목록 |
| GET | `/api/v1/documents/{doc_id}/view` | 문서 Signed URL |
| DELETE | `/api/v1/documents/{doc_id}` | 문서 삭제 |
| GET | `/api/v1/cases/{id}/label-images` | 라벨 이미지 메타 (F4 연동) |
| GET | `/api/v1/cases/{id}/parsed-result/export.{docx\|pdf}` | OCR 결과 다운로드 |

### 2-5. F0 내부 동작 순서 (parse 기준)

```
POST /api/v1/cases/{id}/parse
  │
  ├─1. cases 테이블에서 case 존재 확인, product_name 힌트 추출
  │
  ├─2. documents 테이블에서 해당 case의 모든 업로드 문서 조회
  │
  ├─3. 각 문서별:
  │      ├─ Supabase Storage에서 파일 바이트 다운로드
  │      ├─ MIME/확장자로 포맷 분기
  │      └─ ocr_service.extract_text_from_file() 호출
  │            ├─ PDF  → PyMuPDF → (스캔이면 Vision fallback)
  │            ├─ 이미지 → Vision(gpt-4o)
  │            ├─ HWP  → parser-service HTTP
  │            └─ XLSX → openpyxl
  │
  ├─4. doc_type별 Raw 텍스트 병합 → raw_texts 딕셔너리 완성
  │
  ├─5. parsing_service.parse_raw_texts_to_structured() 호출
  │      ├─ SYSTEM_PROMPT에 파싱 규격 + 공정코드 211개 테이블 주입
  │      ├─ GPT-4o에 raw_texts 전체 투입 (response_format=json_object)
  │      └─ 반환된 JSON → ParsedResult Pydantic 검증
  │
  ├─6. documents 테이블의 parsed_md에 Raw 텍스트 저장
  │
  ├─7. pipeline_steps 테이블에 step_key='0'으로 UPSERT
  │      └─ ai_result = ParsedResult.model_dump()
  │
  ├─8. cases.current_step = '0' UPDATE
  │
  ├─9. (doc_type='label'이 있으면) 백그라운드에서 라벨 이미지 재처리
  │      └─ label_image_service.process_label_image()
  │            ├─ Vision으로 bbox + 텍스트 동시 추출
  │            ├─ PIL 크롭 → PNG → Storage 업로드
  │            └─ case_label_images INSERT
  │
  └─10. ParseResponse 반환 (parsed_result + raw_texts + 에러 목록)
```

### 2-6. F0가 왜 중요한가

#### 1) 파이프라인의 **단일 진입점**이자 **데이터 소스 오브 트루스**

F1~F5 어떤 기능도 원본 파일을 직접 읽지 않습니다. 모두 `pipeline_steps.step_key='0'`의 `ai_result` JSONB를 조회해서 씁니다. 즉, **F0가 만들어낸 JSON = 파이프라인 전체의 공통 입력값**입니다.

#### 2) F0 오류 = 하류 전체 오류의 증폭

F0의 실수는 아래처럼 번집니다:

| F0 오류 | 하류 파급 |
|--------|----------|
| INS·CAS 누락 | F1이 동의어 매칭 실패 → 허용 원료를 금지로 오판 |
| 공정코드 오매핑 | F1/F2가 잘못된 식품유형·기준치 적용 |
| 복합원재료 하위 성분 누락 | 숨은 금지 원료 탐지 불가 (f0_수정_요청_사항 #2) |
| 알코올 도수 누락 | F1이 주류 전용 기준치로 분기 못함 (f0_수정_요청_사항 #3) |
| 사용 부위 누락 | F1이 조건부 원료("잎만 허용") 판정 불가 (f0_수정_요청_사항 #1) |
| 제조사/내용량 누락 | F4·F5가 라벨 시안 생성 불가 (f0_수정_요청_사항 #4, #5) |
| 라벨 이미지 크롭 실패 | F4 Vision 분석 불가 |

#### 3) 비용·성능의 병목

OCR + LLM 파싱은 **전체 파이프라인에서 가장 비싼 단계**입니다. 한 케이스당 Vision API 호출이 수회, Chat API 호출이 1회 이상 발생합니다. F0가 효율적이지 않으면 하류 전체가 느려지고 비용이 커집니다.

#### 4) 포맷 호환성의 최전선

HWP, 스캔본 PDF, Excel 수출입 양식 등 **파일 포맷의 혼란상을 유일하게 흡수하는 레이어**입니다. F1~F5는 모두 깨끗한 JSON만 다루면 됩니다. 한국 검역 실무에서 HWP/HWPX 비중이 높아 이 부분의 견고성이 서비스 전체의 품질을 좌우합니다.

#### 5) 사용자 신뢰의 첫인상

사용자가 파일을 올린 직후 F0 결과 편집기(`OcrResultEditor`)를 봅니다. 이 화면이 엉터리면 사용자는 그 뒤 F1~F5 판정을 아예 신뢰하지 않습니다. 즉 **제품 신뢰도의 첫 관문**입니다.

---

## 3. F0 현재 작업 현황

### 3-1. 완료된 것

- FastAPI 라우터(`upload.py`, `cases.py`) + 서비스(`ocr_service.py`, `parsing_service.py`, `label_image_service.py`, `export_service.py`) 구현
- 4개 문서 포맷 지원 (PDF/이미지/HWP/Excel)
- OpenAI GPT-4o 기반 통합 파싱
- 라벨 이미지 bbox 크롭 + 6개 텍스트 필드 추출
- DOCX/PDF 내보내기
- Supabase `case_label_images` 테이블 v1→v2→v3 마이그레이션 적용
- F4 연동 엔드포인트 (`/label-images`) 제공
- `F0_OPENAI_API_KEY` 단일 키 운용으로 환경변수 정리 (Pinecone·Anthropic·Parser 서비스 키 제거)

### 3-2. 작업 대기 (f0_수정_요청_사항.md)

F1 병합 과정에서 요청받은 6개 항목:

| # | 항목 | 긴급도 | 수정 위치 |
|---|------|-------|----------|
| 1 | `IngredientItem.part` — 사용 부위 추출 | 중간 | `schemas/upload.py` + `parsing_service.py` 프롬프트 |
| 2 | `IngredientItem.sub_ingredients` — 복합원재료 재귀 파싱 | **높음** | 위 두 파일 |
| 3 | `BasicInfo.alcohol_percentage` — 알코올 도수 | **높음** | 위 두 파일 |
| 4 | `BasicInfo.content_volume` — 내용량 (F4 요청) | 중간 | `schemas/upload.py` |
| 5 | `BasicInfo.manufacturer` — 제조사 (F4 요청) | 중간 | `schemas/upload.py` |
| 6 | MIME 타입 검증 구현 | 낮음 | `routers/upload.py` |

※ 이 중 일부(공정코드 구조, 복합원재료 스키마)는 `parsing_service.py`의 SYSTEM_PROMPT에 이미 반영 완료 상태.

### 3-3. 최근 해결한 이슈

- **환경변수 로딩 실패**: `parsing_service.py`가 모듈 import 시점에 `F0_OPENAI_API_KEY`를 읽고 캐시해서, uvicorn --reload가 `.env` 변경을 감지하지 못하는 문제. `_call_openai` 함수 호출 시점마다 `load_dotenv(override=True)` + 런타임 env 재읽기로 수정 완료.
- **.env 인코딩 문제**: Windows 메모장 기본 ANSI(cp949) 저장으로 한글 주석이 깨져 `python-dotenv` 파싱 실패. ASCII 전용 포맷으로 재작성.

---

## 4. 요약

**F0는 "서류라는 혼돈"을 "JSON이라는 질서"로 변환하는 층**이다. 파이프라인의 첫 단계이면서 동시에 하류 5개 기능이 공통으로 의존하는 단일 입력 지점이므로, F0의 정확성·견고성·속도가 곧 서비스 전체의 정확성·견고성·속도가 된다. 파일 포맷의 복잡성을 혼자 떠안고, 하류에는 오직 깔끔한 JSON만 넘긴다는 것이 F0의 설계 철학이다.
